import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument } from 'mongoose';

export type {{properCase name}}Document = HydratedDocument<{{properCase name}}>;

@Schema()
export class {{properCase name}}{

}


export const {{properCase name}}Schema = SchemaFactory.createForClass({{properCase name}});